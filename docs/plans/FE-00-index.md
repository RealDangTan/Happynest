# FE WORKSTREAM — INDEX & STATUS BOARD

> **Owner:** Session FE (build) · **Spec:** [`delivery-design-spec.md`](delivery-design-spec.md) §4 · **Contract:** [`delivery-contracts.md`](delivery-contracts.md)
> **Lãnh thổ:** chỉ được viết `frontend/` + các file `FE-*` + `13–16-*` + `backend/`. Cấm đụng `UF-*`, `AGENTS.md`, file chung ngoài quy tắc append+re-read (xem [delivery-execute-plan.md](delivery-execute-plan.md) §2).
> **Quy tắc just-in-time:** plan chi tiết của từng dòng "☐ chưa có plan" được VIẾT NGAY TRƯỚC khi thực thi, theo khuôn mẫu §3 roadmap. Đánh số task trong plan theo TDD: test trước → chạy thấy fail → code tối thiểu → pass → commit.

## Bảng plan

| # | File | Phạm vi | Viết lúc | Blocked by | Status |
|---|---|---|---|---|---|
| 01 | [FE-01-init-scaffold.md](FE-01-init-scaffold.md) | Node bump check; init shadcn vào `frontend/`; proxy rewrites; TanStack Query provider; `lib/api.ts` + Vitest | ✅ đã viết | Node ≥20.18.1 | ✅ xong 2026-08-25 |
| 02 | [FE-02-auth-shell.md](FE-02-auth-shell.md) | Middleware guard; `/login`; shell Sidebar; hook `/auth/me` | ✅ đã viết | FE-01 | ✅ xong 2026-08-25 |
| 03 | [FE-03-feedback-screens.md](FE-03-feedback-screens.md) | List + filters URL params; detail + similar; import CSV dialog | ✅ đã viết (JIT 2026-08-25) | FE-02 | ✅ xong 2026-08-25 |
| 03b | [FE-03b-source-columns-csv-map.md](FE-03b-source-columns-csv-map.md) | Cải tiến theo feedback owner: cột bật/tắt localStorage; registry nguồn BE nhẹ + wizard đăng ký; CSV map cột + preview | ✅ đã viết (JIT 2026-08-25, duyệt qua [decisions](../decisions.md)) | FE-03 | ✅ xong 2026-08-26 |
| 04 | [FE-04-analysis-runs.md](FE-04-analysis-runs.md) | Trigger run + progress polling + results table | ✅ đã viết (JIT 2026-08-25) | FE-03 | ✅ xong 2026-08-26 |
| 05 | [FE-05-hitl-review-ui.md](FE-05-hitl-review-ui.md) | Queue pending; Dialog edit/AlertDialog reject; corrections UI (BE 13 đã thật — bỏ mock) | ✅ đã viết (JIT 2026-08-26) | FE-04, UF-04, plan 13 | ✅ xong 2026-08-26 |
| 06 | [FE-06-clusters-page.md](FE-06-clusters-page.md) | Trang /clusters nối API thật plan 14 (P3). Insights + Reports + dashboard P4 tách sang FE-06b khi BE plans 15–16 ship | ✅ đã viết (JIT 2026-08-26) | FE-04, UF-05, plan 14 | ✅ xong 2026-08-26 |
| 06b | [FE-06b-insights-reports-dashboard.md](FE-06b-insights-reports-dashboard.md) | Insights + Reports + dashboard đầy đủ (UF-05 Màn 2–4, pha P4) | ✅ đã viết (JIT 2026-08-26) | FE-06, plans 15–16 | ✅ xong 2026-08-26 |
| 07 | FE-07-polish-demo.md | `[tuỳ chọn]` dark-mode toggle, empty states tinh gọn, data demo chuẩn bị bảo vệ | đầu P5 | tất cả | ☐ |
| 08 | [FE-08-auth-register-google.md](FE-08-auth-register-google.md) | Đăng ký email/mật khẩu (default operations) + logout + nút Google ghost (làm thật khi có GCP credentials) | ✅ đã viết (JIT 2026-08-28) | FE-03 | 🔄 đang làm 2026-08-28 |

## Quy ước bắt buộc mọi plan/code FE

- shadcn skill rules: `gap-*` không `space-y-*`; form dùng `FieldGroup`/`Field`; Empty/Skeleton/Badge thay markup tự chế; semantic colors (`bg-primary`, `text-muted-foreground`); icon qua `data-icon`; Dialog/Sheet luôn có Title.
- Add component bằng `pnpm dlx shadcn@latest add <tên>` ĐÚNG lúc cần — không `add --all`.
- Data fetch qua wrapper `lib/api.ts` (FE-01 định nghĩa) — không fetch tay rải rác.
- Filter/pagination là URL search params; mutation xong invalidate query key tương ứng.
- Mỗi task kết thúc = commit nhỏ conventional.

## Tiến độ log (append-only)

- 2026-08-25 — tạo board; FE-01/FE-02 viết đủ chi tiết (đợt 1). — claude-code
- 2026-08-25 — thực thi xong FE-01 + FE-02 (10 commit, `pnpm build` xanh, vitest 3/3). Lệch so với plan cần biết khi đọc lại plan: (1) scaffold shadcn **không có `src/`** — mọi path `src/app|src/lib|src/hooks` trong plan hiểu là `app|lib|hooks` ở root `frontend/`, alias `@/*` → `./*`; (2) template tự init `.git` con + kèm AGENTS.md riêng → đã xoá cả hai; (3) pnpm sau bump Node v24 đòi purge node_modules và chết khi không TTY → chạy với `CI=true`; (4) TaskStop shell bash không kill tiến trình node con của `next dev` trên Windows → phải `taskkill /PID … /F`, server cũ còn chiếm :3000 gây verify sai; (5) `/login` lúc verify middleware trả 404 (chưa có trang) thay vì 200 như plan ghi — hành vi middleware vẫn đúng, trang được tạo ở FE-02 T3. — claude-code
- 2026-08-25 — thực thi xong FE-03 (4 task, build xanh, verify live qua proxy :3000). Bằng chứng: list HTTP 200 + filter URL params; import CSV trả `{imported:2,failed:1,errors:[{row,reason}]}` khớp UI; detail HTTP 200 sau khi restart dev-server sạch. Lệch so với plan: (1) plan thiếu `tabs` trong lệnh shadcn add → bổ sung `pnpm dlx shadcn@latest add tabs`; (2) lỗi kiểu TanStack Query `error` mặc định là `Error` → phải `instanceof ApiError && status===404`; (3) dev server sống từ thời FE-02 bị corrupt SSR worker ("Jest worker exceeded retry limit" → HTTP 500 trên trang detail) → restart sạch hết lỗi; (4) pytest integration của phiên song song ghi đè mật khẩu seed lần nữa → re-run `uv run python scripts/seed_users.py` trước verify; (5) phát hiện + vá: `use-mobile.ts` (dependency của `sidebar.tsx`) chưa từng được commit từ FE-02 → commit riêng `fix(frontend)`. — claude-code
- 2026-08-26 — thực thi xong FE-03b (5 task, duyệt scope qua decisions.md cùng ngày 2026-08-25). Bằng chứng: alembic 0005 upgrade/downgrade sạch; pytest sources 5/5; vitest 7/7 (3 api + 4 csv-mapping); build xanh; import canonical CSV qua proxy `{imported:1}`. Bài học cần nhớ: (1) unit test bắt được bug thật — `findIndex` trên mảng alias rồi dùng làm chỉ mục mảng cột khiến auto-guess trả nhầm cột; test còn chỉ ra thiếu alias tiếng Việt ("ý kiến", "thời điểm") — bổ sung thay vì hạ kỳ vọng; (2) uvicorn KHÔNG chạy --reload → thêm route mới phải restart backend mới thấy (404 "Not Found" dễ hiểu nhầm là sai path); (3) curl.exe `-F file=@...` không đọc được đường dẫn Git Bash `/d/...` (biến thể của quirk /tmp đã ghi ở FE-01) → cd tới thư mục chứa file rồi dùng tên tương đối; (4) mật khẩu seed bị phiên song song re-seed lần thứ 3 — remedy seed_users.py vẫn hiệu quả. Đã đăng ký sẵn 7 nguồn khớp data demo (app_store, app_review, email, google_play, in_app_form, support_ticket, survey) để combobox demo có dữ liệu ngay. — claude-code
- 2026-08-26 — thực thi xong FE-04 (3 task). Bằng chứng live: POST run → 201 `{run_id}`; poll progress thấy `running` 0/3→1/3→2/3→`completed` 3/3 (~52s, LLM thật); results 3 item đều có severity/sentiment/confidence (`ai_issue`=None là nhãn hợp lệ LLM trả về cho feedback ngoài chủ đề AI, KHÔNG phải chưa xử lý — UI hiện "...đang xử lý" cho mọi null theo spec, chấp nhận hạn chế nhỏ này vì FeedbackListOut không mang trạng thái hoàn tất từng item → backlog polish); trang `/analysis?run=` render HTTP 200 qua proxy đủ tiêu đề. Vụ việc môi trường: (1) build chết ở bước static-pages worker exit 0xC0000409 khi dev server vẫn chạy nền trên máy 8GB → kill :3000 rồi build lại là xanh (compile+TS pass cả hai lần); (2) mật khẩu seed bị ghi đè lần thứ 4 bởi pytest phiên song song — email seed đúng là `pm@thesis.local`; (3) gom 4 map nhãn trùng lặp từ feedbacks/page + [id]/page vào `lib/labels.ts`. — claude-code
- 2026-08-26 — thực thi xong FE-05 (5 task; T2+T3 gộp 1 commit vì cùng màn detail đan file). Kèm OQ-3: redirect `/` → `/dashboard` (307 verify qua proxy). Bằng chứng live đầy đủ 4 kịch bản HITL: (1) edit với email giả trong `edited_content` → response `review_status=edited` và nội dung bị Presidio che lại `<EMAIL>` — re-sanitize chạy thật; (2) approve 1-click → approved; (3) POST lại mục đã duyệt → 409 "Thread HITL ... đã hoàn tất" (UI map thành toast + invalidate); (4) correction: body rỗng 422 đúng validator, severity low→critical trả `correction_recorded=true`, review_status KHÔNG đổi. Queue tạo dữ liệu thật bằng ingest feedback chứa PII (email+SDT) → classify xong tự lên pending (severity critical + pii_detected). AC grep include_raw: đúng 1 call site (`use-review.ts`). Mật khẩu seed bị ghi đè lần 5 — remedy cũ vẫn chạy. Backlog mới từ phiên UF: RunProgressOut +4 field snapshot là việc phía session này (theo bảng lãnh thổ), chưa làm. — claude-code
- 2026-08-26 — thực thi xong FE-06 phần Clusters (P3); Insights/Reports/dashboard tách FE-06b chờ BE plans 15–16 (vẫn stub 501). Bằng chứng live: GET trước run → 200 `{"items":[]}` (empty state AC); POST run → 200 C5 `{clusters_upserted:0, assigned_count:0, unassigned_count:28, duration_ms:6744}` — 0 cụm là hành vi ĐÚNG của HDBSCAN trên 28 feedback demo rời rạc (checklist đã cảnh báo, decisions dời evidence P5); trang render 200 qua proxy đủ tiêu đề. Bài học: (1) backend restart lần nữa cần thiết sau commit 3c03206 — uvicorn không --reload; (2) shadcn vega registry KHÔNG có hovercard → dùng popover thay; (3) curl.exe không ghi được `-o /tmp/...` (cùng họ quirk /tmp) → dùng tên file tương đối. Việc còn treo phía session này: RunProgressOut +4 field (backlog BE). — claude-code
- 2026-08-26 — nhặt backlog từ phiên UF: OQ-7 RunProgressOut +4 field snapshot (commit c0b07d0). Chỉ sửa schema (`schemas/analysis.py`) vì route ĐÃ LƯU sẵn giá trị lúc tạo row; test integration mở rộng assert đủ 4 field; verify live GET progress trả `gemini-3-flash` / `text-embedding-3-small` / v1/v1; api-checklist đồng bộ cùng commit. Backlog BE phía session này giờ TRỐNG. — claude-code
- 2026-08-26 — phiên này nhận trọn gói P4 theo owner duyệt qua AskUserQuestion: (1) thực thi BE plan 15 insights API thay stub 501 (commits 9309d5a→a638017, TDD 16 unit + integration, AC cuối dời P5 vì DB dev 0 cụm — decisions cùng ngày); (2) viết JIT plan FE-06b rồi thực thi đủ 5 task (T1 data layer a28afe6 · T2 /insights e18dd24 · T3 /reports 7c2a6da · T4 dashboard 69e6c5f). Bằng chứng live qua proxy :3000: GET `/api/insights` → `{"items":[]}`; POST run khi chưa có cụm → **409** đúng chữ server; C4 days=7 vs 30 khác nhau (136 vs 486 feedback — data demo phiên song song vừa nạp), `by_sentiment` đủ 4 key gồm `mixed`; 3 trang render 200 đúng h1; build xanh 10 route; vitest 10/10 (thêm labels.test cho priorityLabel extract, RED→GREEN). Quyết định thiết kế ghi trong plan: KHÔNG thêm thư viện chart — bar thuần div trên thang token `--chart-1..5` sẵn có (severity đậm dần, critical dùng destructive; sentiment một hue vì phân bố không phải thứ bậc); signature màn insight = khối "Hành động đề xuất" border-l accent. Kịch bản "có cụm" (run thật sinh insight) chờ phiên P5 seed demo dataset chạy clustering xong mới verify được — plan đã lường trước điều kiện này. — claude-code
- 2026-08-28 — thực thi FE-08 (register + logout, Google để ghost) theo plan JIT cùng ngày. BE: POST /api/auth/register (role luôn `operations`, không thêm email-validator — pattern Pydantic) + POST /api/auth/logout; FE: trang `/register` tự login sau 201, nút Google ghost + link qua lại trên cả 2 trang auth, logout menu trên avatar sidebar, middleware cho `/register`. Verify: typecheck sạch, vitest 10/10, build xanh 11 route. **pytest BE BLOCKED** — conftest import chết do voc-os rewrite đang dở xoá model (`app/db/base.py` còn import `correction_example`/`human_review`/`insight`); 8 test mới viết TDD nhưng chưa chạy, chi tiết trong [handoff](../handoffs/2026-08-28-claude-code-fe08-auth-register.md). — claude-code
